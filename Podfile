# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ArticleManagement' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ArticleManagement
  pod 'SnapKit', '~> 3.0.2'
  pod 'ObjectMapper', '~> 2.2'
  pod 'Alamofire', '~> 4.0'
  pod 'SwiftyJSON'
  pod 'Kingfisher', '~> 3.0'
  pod 'YangMingShan'
  target 'ArticleManagementTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ArticleManagementUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
